name: Source formatting

on:
  push:
    branches:
      - main
  pull_request: {} # any target
  schedule:
    - cron: '0 0 * * 0'

jobs:
  format:
    permissions:
      contents: read
      actions: write
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: false
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Set up packages
        run: pip3 install autopep8
      - name: Check code formatting
        run: ./check_format.sh

  flake8:
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install with dependencies
        run: pip3 install flake8 flake8_pyproject
      - name: Run flake8
        run: flake8 .
